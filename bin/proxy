#!/usr/bin/env bash
# Clash for Windows proxy CLI — works in Git Bash and WSL2.

set -euo pipefail

# shellcheck disable=SC1091
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/../lib/proxy-invoke.sh"

if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
    _proxy_cli "$@"
fi
